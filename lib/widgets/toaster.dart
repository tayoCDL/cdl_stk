import 'package:flutter/material.dart';

class Toastr extends StatelessWidget {
  const Toastr({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
